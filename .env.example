# Frontend Environment Variables
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_REACT_APP_BACKEND_URL=https://your-backend-api.com

# Backend Environment Variables (for reference)
MONGO_URL=mongodb://localhost:27017
DB_NAME=your_database_name

# Production URLs (update these when deploying)
# Frontend will be deployed to Netlify
# Backend needs to be deployed to a service like Railway, Render, or Heroku